# Hard-Won Lessons — Don't Relearn These
*Distilled from past failures and fixes. Read before making assumptions.*

## Tools & Capabilities
- **NEVER give up at the first layer.** Check ToolSearch → capabilities.md → CLIs (`which`/`--help`) → Python packages → npm packages → local files → WebSearch → BUILD IT YOURSELF. Only say "can't" after exhausting all 9 options.
- **Use web search for current info.** Don't answer "what's the latest in X" from training data — search first.

## Platform & Infrastructure
- *Add platform-specific gotchas as you encounter them*

## Development Practices
- **Never commit secrets.** `.env` files, API keys, tokens — keep out of git always.
- **Test end-to-end before calling something done.** Expect it to actually work.
- **Save lessons after hard problems.** Update this file when something takes more than one attempt to solve.

## Memory System
- **Auto-memory (these files):** Human-readable, manually curated, always accessible.
- **claude-mem MCP:** Searchable timeline of all past work (if installed).
- **CLAUDE.md:** Always loaded — keep it lean. Only put things that need to be in every session.
- **Memory files:** Loaded on demand — put deeper knowledge here.

## Communication
- **Telegram responses = short.** It's a chat, not an essay. Max 5-6 lines.
- **VS Code responses = can be longer** when building or explaining complex things.
- **Never ask for permission for things already approved.**
